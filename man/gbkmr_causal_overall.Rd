% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06-causal-effects.R
\name{gbkmr_causal_overall}
\alias{gbkmr_causal_overall}
\title{Overall mixture causal dose-response}
\usage{
gbkmr_causal_overall(
  object,
  quantiles = seq(0.3, 0.9, by = 0.05),
  reference = 0.25,
  K = NULL,
  sel = NULL,
  seed = 1L,
  credible_level = 0.95,
  verbose = FALSE
)
}
\arguments{
\item{object}{A fitted \code{gbkmr_results} object.}

\item{quantiles}{Joint mixture quantiles to evaluate.}

\item{reference}{Reference quantile for every mixture component.}

\item{K}{Number of Monte Carlo trajectories per posterior draw. \code{NULL} uses
the value from the fitted model.}

\item{sel}{Posterior iteration indices. \code{NULL} uses the fitted model's
post-burn-in indices.}

\item{seed}{Random seed for g-computation.}

\item{credible_level}{Posterior credible interval level.}

\item{verbose}{Logical; print g-computation progress.}
}
\value{
A \code{gbkmr_causal_effects} object containing tidy \code{summary} and
\code{draws} data frames, the intervention matrix, estimand metadata, and a
ggplot object in \code{plot}.
}
\description{
Estimates the full g-formula contrast between jointly setting every mixture
component to each requested marginal quantile and jointly setting every
component to the reference quantile. Time-dependent confounders are generated
sequentially under every intervention.
}
