% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06-causal-effects.R
\name{gbkmr_causal_bivariate}
\alias{gbkmr_causal_bivariate}
\title{Conditional bivariate causal dose-response curves}
\usage{
gbkmr_causal_bivariate(
  object,
  exposures = NULL,
  time_points = NULL,
  pairs = NULL,
  layout = c("matrix", "wrap"),
  quantiles = seq(0.1, 0.9, by = 0.05),
  conditional_quantiles = c(0.25, 0.5, 0.75),
  reference = 0.25,
  background = 0.5,
  K = NULL,
  sel = NULL,
  seed = 1L,
  credible_level = 0.95,
  verbose = FALSE
)
}
\arguments{
\item{object}{A fitted \code{gbkmr_results} object.}

\item{exposures}{Exposure column names to include. \code{NULL} uses all exposures.}

\item{time_points}{Optional integer visit indices used to filter exposures.}

\item{pairs}{Exposure pairs. The first column or element is the focal
exposure and the second is the conditional exposure. \code{NULL} uses all
ordered pairs when \code{layout = "matrix"} and all unordered pairs when
\code{layout = "wrap"}.}

\item{layout}{Plot layout. \code{matrix} uses focal exposures as columns and
conditional exposures as rows; \code{wrap} uses one panel per pair.}

\item{quantiles}{Joint mixture quantiles to evaluate.}

\item{conditional_quantiles}{Quantiles for the conditional exposure.}

\item{reference}{Reference quantile for every mixture component.}

\item{background}{Quantile at which non-focal exposures are fixed.}

\item{K}{Number of Monte Carlo trajectories per posterior draw. \code{NULL} uses
the value from the fitted model.}

\item{sel}{Posterior iteration indices. \code{NULL} uses the fitted model's
post-burn-in indices.}

\item{seed}{Random seed for g-computation.}

\item{credible_level}{Posterior credible interval level.}

\item{verbose}{Logical; print g-computation progress.}
}
\value{
A \code{gbkmr_causal_effects} object; see
\code{\link[=gbkmr_causal_overall]{gbkmr_causal_overall()}}.
}
\description{
Varies a focal exposure while fixing a second exposure at requested
conditional quantiles. All remaining exposures are fixed at the background
quantile. Each curve is contrasted with the focal reference value under the
same level of the conditional exposure.
}
